.\Cyber_Project\Scripts\activate

# to build the exe it will be in the malware_tools\dist folder, after you run the build_exe.ps1 command
.\build_exe.ps1

# to run the prj in terminal
python orchestrator.py <file_path> --sandbox

# to run the prj with gui in terminal
python gui.py


# when error in env
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process